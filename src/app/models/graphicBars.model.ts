export interface AggregatesBar { //Tipado para grafico de barras
  ticker:string;
  multiplier:number;
  timeSpan:string;
  dateFrom:string;
  dateTo:string;
  adjusted:string;
  sort:string;
  limit:string;
}
